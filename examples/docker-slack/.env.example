ANTHROPIC_API_KEY=sk-ant-your-key-here
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_APP_TOKEN=xapp-your-app-token-here

# Optional — only when routing the Anthropic SDK to a non-default
# endpoint (e.g. MiniMax). If set, also add
# ast.Secret(name="ANTHROPIC_API_URL") to weather_agent.secrets.
# ANTHROPIC_API_URL=https://api.minimax.io/anthropic
