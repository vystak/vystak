# Copy to .env and fill in real values. The file is read by `vystak apply`
# and values are pushed into Azure Key Vault on first apply. Subsequent
# applies skip keys that are already present in the vault unless --force is
# passed.
ANTHROPIC_API_KEY=your-anthropic-api-key-here
